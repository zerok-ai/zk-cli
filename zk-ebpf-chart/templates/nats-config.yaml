apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "zk-ebpf-chart.fullname" . }}-nats-config
  labels:
    app: pl-monitoring
  {{- include "zk-ebpf-chart.labels" . | nindent 4 }}
data:
  nats.conf: {{ .Values.natsConfig.natsConf | toYaml | indent 1 }}