apiVersion: v1
kind: Service
metadata:
  name: pl-nats
  labels:
    app: pl-monitoring
    name: pl-nats
  {{- include "zk-ebpf.labels" . | nindent 4 }}
spec:
  type: {{ .Values.plNats.type }}
  selector:
    app: pl-monitoring
    name: pl-nats
  ports:
	{{- .Values.plNats.ports | toYaml | nindent 2 -}}