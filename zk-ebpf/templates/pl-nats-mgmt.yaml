apiVersion: v1
kind: Service
metadata:
  name: pl-nats-mgmt
  labels:
    app: pl-monitoring
    name: pl-nats
  {{- include "zk-ebpf.labels" . | nindent 4 }}
spec:
  type: {{ .Values.plNatsMgmt.type }}
  selector:
    app: pl-monitoring
    name: pl-nats
  ports:
	{{- .Values.plNatsMgmt.ports | toYaml | nindent 2 -}}