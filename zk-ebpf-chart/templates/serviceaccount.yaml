apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "zk-ebpf-chart.fullname" . }}-metadata-service-account
  labels:
    app: pl-monitoring
    component: vizier
  {{- include "zk-ebpf-chart.labels" . | nindent 4 }}
  annotations:
    {{- toYaml .Values.metadataServiceAccount.serviceAccount.annotations | nindent 4 }}