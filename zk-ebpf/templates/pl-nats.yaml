apiVersion: v1
kind: Service
metadata:
  name: zk-ebpf-nats
  labels:
    app: pl-monitoring
    name: zk-ebpf-nats
  {{- include "zk-ebpf.labels" . | nindent 4 }}
spec:
  type: {{ .Values.plNats.type }}
  selector:
    app: pl-monitoring
    name: zk-ebpf-nats
  ports:
	{{- .Values.plNats.ports | toYaml | nindent 2 -}}