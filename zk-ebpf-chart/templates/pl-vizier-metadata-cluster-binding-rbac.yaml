apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "zk-ebpf-chart.fullname" . }}-pl-vizier-metadata-cluster-binding
  labels:
    app: pl-monitoring
    component: vizier
  {{- include "zk-ebpf-chart.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: '{{ include "zk-ebpf-chart.fullname" . }}-pl-vizier-metadata'
subjects:
- kind: ServiceAccount
  name: '{{ include "zk-ebpf-chart.fullname" . }}-metadata-service-account'
  namespace: '{{ .Release.Namespace }}'